<Menu>        
        <!-- Extension -->
        <DirectoryDir>${productDir}/.shortcuts</DirectoryDir>
        <AppDir>${productDir}.shortcuts</AppDir>
        <Menu>
                <Name>${product.full.name}</Name>
                <Directory>${product}.directory</Directory>
                <Include>
                    <Filename>${product}.desktop</Filename>
                </Include>
        </Menu>
        <!-- End Extension Tools -->
</Menu>